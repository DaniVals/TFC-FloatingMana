<!DOCTYPE html>
<html lang="en">
<head>
	{% set meta_title = "Iniciar sesión" %}
	{% set meta_description = "Inicia sesión en Floating Mana" %}
	
	
	{% include "root.html.twig" %}
	<link rel="stylesheet" href=" {{ asset('styles/form.css') }} ">

</head>
{% include "header.html.twig" %}
<body>

	<div class="container">
		<div class="d-flex justify-content-center align-items-center p-5" style="min-height: 90vh;">
			<form action="{{ path('app_login') }}" method="post" 
			class="row justify-content-center w-100">
			
				{% if responseData is defined %}
					<p class="text-center alert alert-danger">
						{{ responseData.message }}
					</p>
				{% endif %}
				<div class="form-window">
					<label for="_username" class="form-label">Correo electrónico</label>
					<input type="text" id="username" name="_username" class="form-control mb-3"/>

					<label for="_password" class="form-label">Contraseña</label>
					<input type="password" id="password" name="_password" class="form-control  mb-3" />

					<input type="submit" value="Iniciar sesión" class="btn btn-primary d-block w-90">

					<div class="mt-5">
						<a class="btn btn-outline-primary" href="{{ path('app_register_view') }}">Registrarse</a>
					</div>
				</div>
			</form>
		</div>
	</div>
</body>
</html>
