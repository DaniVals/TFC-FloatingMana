<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Log In</title>
	<meta name="description" content="">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	
	
	{% include "root.html.twig" %}
	<link rel="stylesheet" href=" {{ asset('styles/form.css') }} ">

</head>
<body>

	<!-- he añadido esto temporal -->
	{% if responseData is defined %}
		<div class="alert alert-danger" role="alert">
            {{ responseData.message }}
		</div>
	{% endif %}
	<!-- fin de lo añadido temporal -->
	{% if responseData is defined %}
		<div class="alert alert-danger" role="alert">
			{{ responseData.message }}
		</div>
	{% endif %}


	<div class="container">
		<form action="{{ path('app_login') }}" method="post" 
		class="center-in-page-vert row justify-content-center">
			<table class="col-12 col-md-8 pop-up-bordered">
				<tr class="p-5">
					<td class="col-6 col-md-4 p-2">Usuario</td>
					<td class="col-6 col-md-4 p-2"><input type="text" id="email" name="_email" class="form-control" /></td>
				</tr>
				<tr>
					<td class="p-2">Contraseña</td>
					<td class="p-2"><input type="password" id="password" name="_password" class="form-control" /></td>
				</tr>
				<tr>
					<td colspan="2" class="p-2 text-center"><button type="submit" class="btn btn-primary">Iniciar sesión</button></td>
				</tr>
				<tr>
					<td colspan="1" class="mb-1 text-center"> <a href="{{ path('app_register_view') }}">Registrarse</a> </td>
					{#<td> <a href="{{ path('app_recovery_view') }}">Recover Password</a> </td>#}
				</tr>
			</table>
		</form>
	</div>
</body>
</html>
